import React from 'react';
import logo from '../logo.svg';

const NotFound = () => (
  <div>
    <img src={logo} className="App-logo" alt="logo"/>
    <h1>Error 404...the web-page does not exists.</h1>
  </div>
);

export default NotFound;
